/**
 * @mf-dashboard/shared
 *
 * Exports shared utilities and types used across all micro-frontends.
 * Only put truly cross-cutting concerns here — each MF should own its domain logic.
 */

export * from './types';
export * from './utils';
